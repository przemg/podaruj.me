# Animated Demo (v2) — Initial Prompt

Create an animated HTML marketing demo showing how Podaruj.me works.

IMPORTANT: The demo must look like a simplified version of the ACTUAL app — same colors, same layout style, same rounded cards, same pastel warm aesthetic. Look at the real app components and pages for reference. Don't invent a new design — simplify what already exists.

The animation should auto-loop through these scenes with smooth transitions:

1. LANDING — Desktop browser mockup showing the actual Podaruj.me hero section, simplified.
2. CREATE LIST — Simplified version of the real create list form.
3. ADD GIFTS — Items appear in cards that look like the real item cards from the app.
4. SHARE — The real share options appear: copy link, email, QR code.
5. GUEST VIEW — Same card style but from guest perspective. Guest reserves an item.
6. PRIVACY MODES — Show the three real privacy mode cards.
7. COUNTDOWN & CLOSE — Countdown timer, confetti, summary card.
8. END — Podaruj.me logo with tagline.

Style: Desktop browser mockup frame, matching actual app visual identity, smooth CSS animations, auto-playing infinite loop, 30-40 seconds total.

Output as a single HTML file saved to `public/demo/demo2.html`.
