# Google Auth - Initial Prompt

Read CLAUDE.md and PROJECT.md.

Add Google sign-in as the primary login method alongside existing magic link.

On the sign in page:
- Add a big "Sign in with Google" button above the existing magic link form
- Visual separator between them ("or continue with email")
- Google button should feel like the primary/recommended option

Make sure Google login creates a user profile in the database the same way magic link does (name and avatar from Google account).

Update PROJECT.md auth section to reflect Google as primary method.

Save initial prompt to docs/auth-google/prompt.md.
Follow Superpowers workflow and End of Task Checklist from CLAUDE.md.
